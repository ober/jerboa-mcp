{
  "name": "jerboa-mcp",
  "version": "1.0.0",
  "description": "MCP server providing Jerboa language intelligence (Gerbil Scheme on Chez)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "copy-resources": "mkdir -p dist/resources && cp -r src/resources/* dist/resources/ 2>/dev/null || true",
    "build": "tsc && npm run copy-resources",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  }
}
