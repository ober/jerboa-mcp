[
  {
    "id": "static-build-lso-guard-audit",
    "title": "Audit Scheme files for unguarded load-shared-object calls that crash static binaries",
    "description": "Scan .ss/.sls files and report any top-level load-shared-object or foreign-procedure calls that are not guarded by a JEMACS_STATIC (or similar) env-var check. Unguarded calls crash static binaries at boot time. Should report: file, line, the unguarded call, and whether a guard pattern exists nearby.",
    "impact": "medium",
    "tags": [
      "static",
      "build",
      "load-shared-object",
      "ffi",
      "audit",
      "musl"
    ],
    "use_case": "When preparing a Jerboa project for static binary compilation (musl or otherwise), before spending 30+ minutes on a Docker build that fails at runtime.",
    "example_scenario": "Building jemacs-qt static: (chez-qt ffi) calls load-shared-object at top level. Without JEMACS_STATIC=1 the compilation step fails with a dlopen error inside the container. Finding this required reading vendor/chez-qt/chez-qt/ffi.ss manually. A tool scan would have flagged it immediately.",
    "estimated_token_reduction": "~800 tokens per static build session (eliminates 3-5 grep/read cycles to find unguarded FFI calls)",
    "votes": 1
  },
  {
    "id": "doc-code-block-verify",
    "title": "Verify Jerboa code blocks inside markdown docs compile and run",
    "description": "Scan a .md file (or directory) for fenced ```scheme / ```jerboa code blocks, extract each one, run it through the compiler, and optionally evaluate it. Report per-block status: compiled/failed/ran/threw, with line number of the fence and the exact error. Support an `:imports` metadata line before the fence (or a doc-level header) so the tool knows which modules each block expects. For blocks that are partial (no imports, no top-level wrapping), auto-wrap in `(import (jerboa prelude)) ...`. Intended for keeping design docs, cookbook sources, and README examples in lockstep with reality.",
    "impact": "high",
    "tags": [
      "doc",
      "markdown",
      "verify",
      "code-block",
      "drift",
      "example"
    ],
    "use_case": "When shipping a design doc or porting guide that contains many worked code examples, before committing. Also for periodic audits of existing docs to catch drift when APIs change. Would have prevented shipping docs/core-async.md with two broken references (`@out` reader sugar that doesn't exist, `(swap! out cons v)` with wrong argument order) — both only discovered by running the example manually after commit.",
    "example_scenario": "docs/core-async.md Section 17 contains a 20-line worked example that ports Clojure's scatter/gather pattern to Jerboa. Two lines use `@out` (Clojure atom-deref reader sugar that doesn't exist in Jerboa) and one uses `(swap! out cons v)` instead of `(swap! out (cut cons v <>))`. A `jerboa_doc_verify docs/core-async.md` call would have compiled the block, noted \"variable @out is not bound\" at the fence's line, and caught both errors in one shot. Instead I wrote a /tmp/test-async-example.ss file, ran it by hand, saw the first error, fixed it, re-ran, saw the second, fixed it, re-ran again. Three iterations that one tool call could collapse into one. Same friction applies to every cookbook recipe on disk and every README code snippet.",
    "estimated_token_reduction": "~1500 tokens per doc-ship session (replaces: write tmp file → read → run → parse error → re-write → re-run → parse → fix → re-run cycle). Multiplicative across every code block in a large doc.",
    "votes": 2
  },
  {
    "id": "jerboa-safe-prelude-default",
    "title": "Make safe APIs the default prelude exports",
    "description": "The (jerboa prelude) should re-export safe-sqlite-open as sqlite-open, safe-tcp-connect as tcp-connect, etc. Claude and humans will always reach for the simplest import — if the safe version IS the default, nobody has to remember to use (std safe). Raw APIs should require explicit (import (std db sqlite-native)) opt-in.",
    "impact": "critical",
    "tags": [
      "safety",
      "prelude",
      "default",
      "claude",
      "api",
      "import"
    ],
    "use_case": "Every time Claude writes Jerboa code, it imports (jerboa prelude). If that gives it the safe APIs by default, all generated code is automatically type-checked and resource-safe without Claude knowing about the safety modules.",
    "example_scenario": "Claude writes (import (jerboa prelude)) then (sqlite-open \"db\") — this silently calls safe-sqlite-open which validates types, and (with-resource ...) is the documented pattern. No separate (import (std safe)) needed.",
    "estimated_token_reduction": 200,
    "votes": 1,
    "note": "Requires Jerboa core change (prelude.sls), not implementable in jerboa-mcp."
  },
  {
    "id": "ai-prompt-to-jerboa-code",
    "title": "jerboa_prompt_to_code: Generate verified Jerboa from natural language prompts",
    "description": "Tool that takes pseudocode/prompts, generates Jerboa via LLM, then verifies with eval/syntax_check/verify loop, iterating until passing. Outputs working .ss snippet.",
    "impact": "high",
    "tags": [
      "ai",
      "codegen",
      "verification",
      "prompt"
    ],
    "use_case": "AI assistants generating safe, executable Scheme from user descriptions without manual fixes.",
    "example_scenario": "User: \"Write a function to sort a list and compute sum.\" Tool: Generates def/sort/sum code, verifies, returns verified snippet.",
    "estimated_token_reduction": "~800 tokens per code gen session (auto-verify reduces back-and-forth).",
    "votes": 0,
    "note": "Requires calling an LLM API from within an MCP tool — not practical without Anthropic API integration."
  },
  {
    "id": "jerboa-ai-scaffold-ml",
    "title": "jerboa_ai_scaffold: Generate ML/AI patterns from Rust crates",
    "description": "Scaffold Jerboa FFI wrappers for Rust ML libs (e.g., tch-rs tensors, ndarray stats); auto-generates safe bindings with type checks and examples.",
    "impact": "high",
    "tags": [
      "ai",
      "ml",
      "ffi",
      "rust",
      "scaffold"
    ],
    "use_case": "Prototyping AI models in Jerboa without manual FFI boilerplate.",
    "example_scenario": "User: \"Scaffold tensor ops from tch-rs.\" Tool: Generates foreign-procedure defs, safe wrappers, test suite.",
    "estimated_token_reduction": "~1200 tokens (avoids explaining/generating FFI from scratch).",
    "votes": 0,
    "note": "Requires LLM API + knowledge of Rust crate APIs — too domain-specific without crate introspection tooling."
  },
  {
    "id": "jerboa-semantic-search-stdlib",
    "title": "jerboa_semantic_search: Embeddings-based search over stdlib and cookbooks",
    "description": "Vector search for functions/patterns by intent (e.g., \"safe file read\"); uses simple embeddings on docstrings/exports for code discovery.",
    "impact": "high",
    "tags": [
      "search",
      "semantic",
      "ai",
      "discovery"
    ],
    "use_case": "AI finding relevant APIs without exact name knowledge.",
    "example_scenario": "Query: \"Handle JSON errors safely.\" Returns: json-parse with try/catch examples from cookbook.",
    "estimated_token_reduction": "~600 tokens (reduces grep/howto trial-and-error).",
    "votes": 0,
    "note": "Keyword-based version implemented as jerboa_stdlib_search. True semantic/embeddings search requires a vector store or embedding API."
  },
  {
    "id": "detect-worker-shared-mutation",
    "title": "Scanner to detect worker-thread mutation of shared hash tables and lists",
    "description": "Add a static analysis pass that flags the anti-pattern where a (spawn ...) worker thread's body (including parameterized callbacks) calls hash-put!, hash-remove!, set-car!, set-cdr!, or struct setters on values that are ALSO read by the thread that called spawn. This is unsafe under Chez preemptive pthreads — hash tables and pair lists are not thread-safe, and empirical testing shows 33–37% data loss on concurrent mutation even with per-op mutexes. The scanner should recommend the mailbox pattern (thread-send / non-blocking thread-receive 0 drain) as the fix.",
    "impact": "high",
    "tags": [
      "thread-safety",
      "race",
      "spawn",
      "hash-put",
      "concurrency",
      "lint"
    ],
    "use_case": "Any Jerboa project using (spawn ...) with worker threads that drive a UI or long-running loop. The bug is invisible in development (messages appear to work) but manifests as silent data loss or hangs under load. A static pass that flags this pattern at lint time would save hours of debugging per occurrence.",
    "example_scenario": "jerboa-code TUI spawned a worker thread that called (hash-put! tool-counts name ...) and mutated msg-block lists via a parameterized callback. The main event-loop thread read these same structures to render each frame. Under load the TUI would hang randomly, losing ~1 in 3 events. Took Gemini 3.1 Pro pseudocode analysis to diagnose. A linter that recognized \"callback defined inside parameterize inside spawn\" + \"mutates value reachable from outer scope\" would have flagged this immediately.",
    "estimated_token_reduction": "~3000 tokens per occurrence (avoids multi-round debugging session, .log analysis, thread-safety recipe search)",
    "votes": 0
  },
  {
    "id": "batch-symbol-existence-check",
    "title": "Batch symbol-existence check across prelude and (std ...) modules",
    "description": "A tool that takes an array of symbol names (function/macro/variable names) and returns, for each, whether it exists anywhere in `(jerboa prelude)` + all `(std ...)` modules, with its defining module, kind (proc/macro/syntax/var), and arity if known. Equivalent to running `jerboa_apropos` (or the existing function_signature tool) N times in parallel, but in a single call. Results would look like `[{name: \"merge-with\", exists: false}, {name: \"select-keys\", exists: true, module: \"(std clojure)\", kind: \"proc\", arity: \"2\"}, ...]`. Should default to an exact-name lookup, with an optional `fuzzy` flag to also include close matches.",
    "impact": "medium",
    "tags": [
      "apropos",
      "symbol",
      "lookup",
      "batch",
      "existence"
    ],
    "use_case": "When updating documentation that enumerates whether a set of external-ecosystem names (Clojure's, Racket's, R6RS's) are actually present in Jerboa. Also during porting work, when you need to quickly know which names from a source file will resolve vs need polyfills. Any time the question is \"do these N functions exist in Jerboa?\"",
    "example_scenario": "While updating `docs/clojure-vs-jerboa.md` to add status markers, I needed to verify whether Clojure names like `merge-with`, `zipmap`, `reduce-kv`, `memoize`, `iterate`, `repeatedly`, `fnil`, `every-pred`, `some-fn`, `trampoline`, `min-key`, `max-key`, `min-by`, `max-by`, `iterate-n` exist in Jerboa. I ran 5 separate `Grep` calls across `lib/` to check — a total of ~15 symbol lookups. A single `jerboa_symbol_exists_batch` call with the list would have returned `{select-keys: exists, merge: exists, everything-else: absent}` in one shot. Also discovered that the existing doc had several stale claims about non-existent functions (iterate-n, min-by, max-by, fnil-in-prelude, every-pred-in-prelude) because no one had cheap way to sanity-check a batch of names.",
    "estimated_token_reduction": "~600 tokens per doc-verification pass (replaces 4-6 grep calls + result inspection with 1 structured tool call). Higher for porting workflows that cross-check dozens of names.",
    "votes": 2
  },
  {
    "id": "detect-fork-in-fiber-context",
    "title": "Detect fork-unsafe calls (run-safe-eval, subprocess) inside fiber handlers",
    "description": "Static analysis pass that flags calls to run-safe-eval, open-process-ports, or other fork()-using APIs when they appear inside HTTP handler functions, transducer lambdas (filtering/mapping bodies), or channel callbacks (go blocks, fiber thunks). These contexts are fibers — fork() from a fiber silently corrupts the scheduler. The tool should trace the call graph: if handler-fn calls eval-fn which calls run-safe-eval, flag the whole chain. Should report the fiber entry-point (e.g., route-get handler name) and the unsafe call site.",
    "impact": "high",
    "tags": [
      "fork",
      "fiber",
      "run-safe-eval",
      "scheduler",
      "static-analysis"
    ],
    "use_case": "Any service using fiber-httpd or CSP channels that wants to use sandboxed eval for user-provided code. The fork-in-fiber bug produces zero error output — events simply stop processing. Diagnosing it requires knowing the fork/fiber interaction, which is non-obvious.",
    "example_scenario": "jerboa-edge Phase 2: eval-user-filter was initially implemented using run-safe-eval inside the transducer (which runs during fiber >!! calls). Filters appeared to register but never dropped events. No error logged anywhere. Root cause only found after ~15 tool calls investigating the filter logic, format strings, and alist key types — all red herrings. Switching to restricted-eval-string fixed it immediately. A static flag at authoring time would have prevented the entire debugging session.",
    "estimated_token_reduction": "~3000 tokens per occurrence (eliminates multi-round silent-failure debugging: format-string investigation, alist-key investigation, eval environment investigation — all red herrings for a scheduler corruption bug)",
    "votes": 0
  },
  {
    "id": "auto-balance-check-after-deep-edit",
    "title": "Auto-run balance check after edits to functions with nesting depth &gt; 4",
    "description": "When jerboa_balanced_replace, jerboa_splice_form, or jerboa_wrap_form edits a function whose body has nesting depth exceeding a threshold (e.g., 4 levels of let/cond/match nesting), automatically run jerboa_check_balance on the edited region and return any mismatch warnings inline with the edit result. This prevents the failure mode where a deep edit introduces a bracket mismatch that only surfaces as a parse error on the next cold restart — not during the current session where the already-running process masks it.",
    "impact": "medium",
    "tags": [
      "balance",
      "bracket",
      "edit",
      "nesting",
      "parse-error"
    ],
    "use_case": "Any edit to deeply nested Scheme code — especially match+let loop+cond+let* combinations common in actor worker functions. The bug is invisible while the process is running (Chez Scheme re-reads on restart) and only surfaces as a cold-start parse error.",
    "example_scenario": "jerboa-edge make-worker had a real bracket mismatch (close sequence ))]) instead of ))]])) in a 5-level cond-inside-let*-inside-match-inside-let-loop. The balance checker flagged it each session but it was dismissed as a heuristic warning because the already-running service responded to health checks. On the next cold restart: Exception in read: bracketed list terminated by parenthesis. The entire Phase 2 filter testing session was on a service running from stale code — the fix hadn't taken effect at all.",
    "estimated_token_reduction": "~500 tokens per deep edit (eliminates the restart→parse-error→debug cycle; inline warning at edit time is 1 line vs 3-5 tool calls to diagnose the cold-start failure)",
    "votes": 0
  },
  {
    "id": "module-doc-with-calling-convention-example",
    "title": "jerboa_doc should include a minimal working usage example for each exported symbol",
    "description": "jerboa_doc and jerboa_module_catalog currently return type, arity, and qualified name — but not a usage example. For modules with non-obvious calling conventions (argument order, overloaded variadic signatures, registry patterns), arity alone is misleading and forces reading the full stdlib source. A short 2-5 line example showing the correct call pattern would prevent the wrong-arg-order failure mode entirely.",
    "impact": "high",
    "tags": [
      "doc",
      "example",
      "calling-convention",
      "module-catalog",
      "arity",
      "usage"
    ],
    "use_case": "When writing code that uses a stdlib module for the first time, especially modules with variadic signatures or unusual argument ordering (registry-first, config-last, etc.).",
    "example_scenario": "Implementing (std metrics) in jerboa-edge Phase 3: make-counter reported \"3 or more arguments (variadic, arity mask: -8)\" — unhelpful. Three successive jerboa_eval attempts failed (registry last, then registry as #f, then keyword syntax) before reading the full 200-line stdlib source to discover that registry is the FIRST argument: (make-counter reg name help [label-names]). A jerboa_doc call returning \"(make-counter reg name help) ;; reg is the registry\" would have resolved this in 1 tool call instead of 5 (3 eval + 1 stdlib_source read + 1 eval to confirm).",
    "estimated_token_reduction": "~800 tokens per unfamiliar module (replaces 3 failed evals + stdlib source read + confirm eval with 1 doc call showing correct pattern)",
    "votes": 1
  },
  {
    "id": "verify-shebang-script-support",
    "title": "jerboa_verify and jerboa_compile_check should handle shebang-line script files",
    "description": "When a .ss file starts with a shebang line (#!/usr/bin/env ... or #!/usr/bin/env -S scheme ...), jerboa_verify reports \"invalid syntax scheme\" and jerboa_compile_check (with force_chez_reader) reports \"invalid syntax #!/usr/bin/env at char 0\". The tools should strip the shebang before analysis, the same way the Chez scheme interpreter does when running --script. Script files (.ss with shebang) are a common deployment pattern for single-file services — all jerboa-edge phases use this form.",
    "impact": "medium",
    "tags": [
      "verify",
      "shebang",
      "script",
      "compile-check",
      "edge"
    ],
    "use_case": "Verifying a single-file Jerboa service that uses a shebang for direct execution (chmod +x edge.ss && ./edge.ss). Currently jerboa_verify always fails on these files, forcing fallback to make check + bash.",
    "example_scenario": "jerboa-edge/edge.ss starts with #!/usr/bin/env -S scheme --libdirs lib --script. Running jerboa_verify on it returns \"Exception: invalid syntax scheme\" — a false positive. The only way to confirm the file is valid is to run make check (which only checks that the scheme binary runs, not the file itself) or start the server via bash. This costs 2-3 extra tool calls per edit cycle.",
    "estimated_token_reduction": "~300 tokens per verify session on script files (eliminates fallback to make check + bash startup test)",
    "votes": 0
  },
  {
    "id": "module-exists-check",
    "title": "Quick check whether a stdlib module path exists without loading it",
    "description": "A lightweight tool that checks whether a module path like (std crdt) or (std actor transport) exists in the Jerboa stdlib, without loading or importing it. Returns exists: true/false plus the .sls file path if found. Currently the only way to check is jerboa_module_exports (which errors loudly if absent) or grepping lib/. The tool should accept both (std ...) and :std/... path forms.",
    "impact": "low",
    "tags": [
      "module",
      "exists",
      "stdlib",
      "check",
      "discovery"
    ],
    "use_case": "When a design doc or plan mentions a module and you need to confirm it exists before writing code that imports it — especially for modules you're less sure about.",
    "example_scenario": "Phase 4 of jerboa-edge planned to use (std crdt) for replicated state. Confirmed non-existence required attempting jerboa_module_exports which returned an error message — one extra tool call plus a confusing error. A jerboa_module_exists (std crdt) returning {exists: false} in 0 tokens of error noise would be cleaner and could be batched with other checks.",
    "estimated_token_reduction": "~150 tokens per check (converts loud error from module_exports into clean boolean; batchable for multiple modules)",
    "votes": 1
  },
  {
    "id": "static-binary-sforeign-symbol-coverage-check",
    "title": "Check that C main Sforeign_symbol registrations cover all foreign-procedure symbols in a WPO binary",
    "description": "Given a C main source file and a set of .sls library files (or a project directory), extract: (1) all `Sforeign_symbol(\"name\", ...)` registrations from the .c file and (2) all `(foreign-procedure \"name\" ...)` strings from the .sls files. Report the diff: symbols referenced by foreign-procedure but NOT registered in Sforeign_symbol will cause \"no entry for name\" crashes at runtime. Also report symbols registered but not referenced (harmless but cluttering). Should optionally cross-check against the actual archive with `nm` to catch wrong symbol names (e.g., `jerboa_sha` vs actual `jerboa_sha256`).",
    "impact": "high",
    "tags": [
      "static-binary",
      "Sforeign_symbol",
      "foreign-procedure",
      "FFI",
      "coverage",
      "musl"
    ],
    "use_case": "When writing or updating the C main for a static Jerboa binary, before spending 30 minutes on a Docker build that then crashes at runtime with \"no entry for getsockname\".",
    "example_scenario": "Building jerboa-edge static binary: the C main had `jerboa_sha`, `jerboa_hmac_sha`, `jerboa_argon`, `jerboa_pbkdf`, `jerboa_chacha` as registered names. The actual archive had `jerboa_sha256`, `jerboa_hmac_sha256`, `jerboa_argon2id_hash`, etc. The linker errored on all of them as undefined references (15+ symbols). Additionally `getsockname` and `getpeername` were used by (std net io) but not registered, causing runtime \"no entry\" crashes. Finding each missing symbol required: (1) Docker build → linker error or runtime crash → (2) grep stdlib source for foreign-procedure uses → (3) nm archive to verify actual name → (4) edit C main → repeat. A coverage check tool would have reported all gaps in one shot before the first build attempt.",
    "estimated_token_reduction": "~2000 tokens per static build (eliminates 4-6 round-trips of build→crash→grep→nm→fix, each ~300-400 tokens)",
    "votes": 0
  },
  {
    "id": "doc-implementation-status-audit",
    "title": "Audit design doc claims against actual file/module existence",
    "description": "Scan a markdown design document for implementation status claims (tables with \"Done\"/\"Complete\" markers, file path references like `src/foo/bar.ss` or `lib/foo/bar.sls`, module path references like `(jerboa-db core)`) and verify each one against the filesystem. Report: (1) files claimed to exist but missing, (2) modules claimed but not importable, (3) status markers (\"Done\", \"Complete\", \"Implemented\") on features with no corresponding source. Returns a diff-style report: \"MISSING: src/jerboa-db/datom.ss (claimed in Phase 1 table, line 452)\" etc.",
    "impact": "high",
    "tags": [
      "doc",
      "audit",
      "status",
      "implementation",
      "design",
      "verify"
    ],
    "use_case": "When reviewing or inheriting a design document that claims implementation status. Prevents shipping docs that say \"All 8 phases complete\" when zero implementation files exist — which wastes entire sessions on false assumptions.",
    "example_scenario": "docs/jerboa-db.md claimed \"All 8 phases implemented (31 files, ~6200 lines, 34/34 tests passing)\" with every scorecard row marked \"Done\". In reality, zero of the 31 src/jerboa-db/*.ss files existed. Discovering this required: (1) grep -r for datom/datomic across lib/src/tests (found nothing), (2) spawning an Explore agent to search comprehensively, (3) manually checking src/jerboa-db/ directory existence. A single `jerboa_doc_status_audit docs/jerboa-db.md` would have returned \"0/31 claimed files exist, 52 features marked Done with no source\" in one call.",
    "estimated_token_reduction": "~2000 tokens per doc review session (eliminates multiple grep/find/agent cycles to verify claims; prevents entire sessions wasted on false assumptions about what's already built)",
    "votes": 0
  },
  {
    "id": "jerbuild-reader-token-validator",
    "title": "jerboa_verify should flag Chez csv10-incompatible reader tokens (#\\escape, #!void)",
    "description": "When validating .ss files destined for jerbuild transpilation, jerboa_verify (and jerboa_check_syntax) should detect token forms that are valid in newer Chez but rejected by csv10 (the Docker/static build toolchain): specifically #\\escape (use #\\esc) and #!void (use (void)). These cause silent build failures — the reader error surfaces only inside Docker, far from the source edit. The tool should report the file, line, offending token, and the correct replacement.",
    "impact": "medium",
    "tags": [
      "jerbuild",
      "reader",
      "csv10",
      "char",
      "void",
      "compat"
    ],
    "use_case": "When editing .ss files that will be transpiled by jerbuild for static/Docker builds using an older Chez csv10 toolchain. The incompatibility is invisible during local development with a newer Chez and only surfaces in Docker.",
    "example_scenario": "A .ss file uses #\\escape as a character literal. Local jerboa_verify passes. The Docker build (Chez csv10) fails with \"invalid character name: escape\" deep in the build log. Finding the source location requires searching all .ss files for #\\escape manually. A jerboa_verify warning would have caught it immediately at edit time.",
    "estimated_token_reduction": "~400 tokens per occurrence (eliminates full Docker build cycle + grep search to locate the offending token)",
    "votes": 0
  },
  {
    "id": "jerbuild-conflict-rule-generator",
    "title": "Tool to compute inter-library conflict rules for jerbuild given a set of co-imports",
    "description": "Given a list of module imports that will be combined with jerbuild's auto-injected (jerboa core) and (jerboa runtime), compute the exact (except ...) clauses needed to avoid duplicate-export errors. The tool inspects the actual export lists of all named modules, intersects them with (jerboa core)'s exports, and returns a ready-to-paste conflict rule alist for jerbuild's *jerboa-core-conflict-rules* table. This replaces the current workflow of: compile → see \"multiple definitions for X\" → grep to find which module also exports X → manually add the rule → repeat.",
    "impact": "medium",
    "tags": [
      "jerbuild",
      "conflict",
      "duplicate-export",
      "jerboa-core",
      "except",
      "inter-library"
    ],
    "use_case": "When adding a new explicit import to a jerbuild-managed .ss file and the generated .sls produces \"multiple definitions for X\" errors. Also useful when initially porting a new Gerbil module to the jerbuild pipeline.",
    "example_scenario": "Porting shell.ss adds (import (std misc string)). The build fails with \"multiple definitions for string-index\". To fix it: check what (jerboa core) exports, check what (std misc string) exports, find the intersection, add an (except (jerboa core) string-index ...) rule. This took 3 tool calls (module_exports x2 + manual diff). A single jerboa_jerbuild_conflict_rules call with the import list would return the complete rule in one shot.",
    "estimated_token_reduction": "~600 tokens per new module addition (replaces 2-3 module_exports calls + manual set intersection + trial-and-error compile cycle)",
    "votes": 0
  },
  {
    "id": "run-tests-project-path-libdirs",
    "title": "jerboa_run_tests: support project_path / extra libdirs like jerboa_make does",
    "description": "jerboa_run_tests currently only knows about the jerboa stdlib libdir. It cannot run tests in projects that have their own lib/ directory (e.g. jerboa-db, jerboa-edge) — it fails immediately with \"library (project-lib core) not found\". jerboa_make works correctly for these projects because it reads the Makefile and passes the right --libdirs. jerboa_run_tests should accept a project_path parameter (like jerboa_make) and auto-derive libdirs from the Makefile, OR accept an explicit extra_libdirs list like [\"lib\", \"/home/user/mine/jerboa/lib\"].",
    "impact": "medium",
    "tags": [
      "run_tests",
      "libdirs",
      "project",
      "test",
      "library"
    ],
    "use_case": "Running tests for any Jerboa project that has its own lib/ directory alongside the jerboa stdlib. Currently must fall back to jerboa_make with target=\"test-X\" which loses the filter/timeout granularity of jerboa_run_tests.",
    "example_scenario": "Working on jerboa-db transport tests: calling jerboa_run_tests with file_path=\"tests/test-transport.ss\" immediately fails with \"library (jerboa-db core) not found\". Had to use jerboa_make with target=\"test-transport\" for every run, losing the ability to filter by test name or set per-test timeouts. If jerboa_run_tests accepted project_path=\"/home/jafourni/mine/jerboa-db\" it could read the Makefile's LIBDIRS variable and pass --libdirs \"lib:/home/jafourni/mine/jerboa/lib\" automatically.",
    "estimated_token_reduction": "~100 tokens per test run (eliminates the fallback round-trip; more importantly, restores filter/timeout capabilities that jerboa_make doesn't expose)",
    "votes": 0
  },
  {
    "id": "detect-blocking-ffi-missing-collect-safe",
    "title": "Lint blocking foreign-procedure declarations missing __collect_safe",
    "description": "Add a static analysis pass that flags `(foreign-procedure \"name\" ...)` declarations where the C symbol's name matches a known-blocking-syscall pattern (regexes like `_read$`, `_recv$`, `_accept$`, `_poll$`, `_wait$`, `_sleep$`, `_lock$`, plus exact matches for recv, accept, poll, ppoll, select, pselect, sleep, nanosleep, etc.) but the declaration lacks the `__collect_safe` keyword. A plain foreign-procedure pins the Chez TC mutex for the duration of the C call, so any blocking syscall freezes every other Scheme thread — the GC, watchdogs, mailbox drains, fiber schedulers — until the call returns. The scanner should report file, line, symbol name, the suspected blocking nature, and recommend declaring it `(foreign-procedure __collect_safe \"name\" ...)` or shadowing the upstream binding locally with __collect_safe.",
    "impact": "high",
    "tags": [
      "ffi",
      "collect-safe",
      "foreign-procedure",
      "blocking",
      "lint"
    ],
    "use_case": "Any Jerboa project that uses fork-thread / spawn for watchdogs, fibers, mailbox drains, or any work concurrent with blocking I/O. Since the Jerboa stdlib (as of 2026-04) declares networking FFI without __collect_safe, callers downstream silently inherit the bug and only discover it under failure modes (peer goes silent, lock contention, etc.) where the watchdog is supposed to fire but cannot.",
    "example_scenario": "jcode hung mid-stream on a deepseek SSE response. The streaming watchdog spawned via fork-thread had a 90s read-timeout — it never fired. Cause: `:std/net/tls-rustls` declares `jerboa_tls_read` as a plain foreign-procedure. While the main thread blocked in recv() for jerboa_tls_read it held TC, so no other Scheme thread (including the watchdog's thread-sleep! return path) could re-enter Scheme. The 90s timer never advanced. Took ~1 hour and several debug instrumentation rounds to diagnose; a linter would have flagged jerboa_tls_read at the first scan and saved the entire investigation. Fix: a private `(foreign-procedure __collect_safe \"jerboa_tls_read\" ...)` shadow in src/jcode/provider/provider.ss.",
    "estimated_token_reduction": "~4000 tokens per occurrence — a TC-mutex hang investigation typically requires a fork-thread test harness, sample-based stack inspection, and reading the FFI source. Catching it at lint time eliminates the entire diagnostic round.",
    "votes": 0
  }
]
